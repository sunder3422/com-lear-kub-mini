apiVersion: v1
kind: Service
metadata:
  name: my-springboot-service
spec:
  selector:
    app: my-springboot-deployment
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: NodePort